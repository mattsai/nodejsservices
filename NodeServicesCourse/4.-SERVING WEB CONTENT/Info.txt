The fastify-static module also decorates the reply object with sendFile method. We can use this to create a route that manually responds with the contents of hello.html if we wanted to alias /hello.html to /hello.


npm install --save-dev fastify-static  contenido estatico fastify
const dev = process.env.NODE_ENV !== 'production'
const fastifyStatic = dev && require('fastify-static')

if (dev) {
    fastify.register(fastifyStatic, {
      root: path.join(__dirname, 'public')
    })
  }

<a hr‌ef='/hello.html'>Hello</a>

node -e "fs.openSync('index.html', 'w')"
node -e "fs.openSync('hello.html', 'w')"

el nombre de los archivo es el nombre de las rutas alaavrg

---------------
la diferencia entre express y fastify

ptm

es que en el fastify 
el engine: {}
{{{}}} 3 
para el bod ene l layout hbs


en express y
express --hbs  y ya genera ahi


express res.rener
fastify reply.view  es imporatnte el return 



streaming content 
es facil en express  solo le pones en false cuando le das el pipe, {}emd:false
ye importar e fnished del stream y con eso jala
bueno y fastify es lo suficientemnte bbergas para saber que es un stream y ya
entonces basta que ke des
const fuente = stream()

return fuente

y ya alv

req.query -> saca los queyr params:D